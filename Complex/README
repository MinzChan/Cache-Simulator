This file contains the code for a Cache Simulator.

How to configure:
(1) Configure Cache
	Change parameters and policies in main.c: Configure().
(2) Select mode
	Change macro in cache.cc to select mode.
	- Naive Mode
		Remove "#define PREFETCH" and "#define BYPASS".
	- Prefetch
		Set "#PREFETCH".
	- Bypass
		Set "BYPASS".

How to run:
(1)	$ sh run.sh
	Will compile and test 1.trace and 2.trace.
(2)	$ make
	$ ./sim SOME_TRACE
